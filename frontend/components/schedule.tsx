"use client";



export default function Schedule({ events }: { events: any[] }) {
    const daysOfWeek = ['MON', 'TUE', 'WED', 'THU', 'FRI', 'SAT', 'SUN'];

    return (
        <div>
            Schedule
        </div>
    )
}